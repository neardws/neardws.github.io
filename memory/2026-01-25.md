- Neil preference/process: Any operations involving ports must first consult the Services Log project (~/User_Services/services-logs) for port usage/availability. Treat it as the source of truth before selecting/exposing ports.

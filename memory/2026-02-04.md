# 2026-02-04 运维日志

## Gateway 宕机事件排查

### 问题时间线
- 2月2日 00:30 - Gateway 首次挂掉，rollback 成功修复
- 2月2日 01:00 - Gateway 再次挂掉，rollback 失败
- 持续宕机 **45+ 小时**，直到手动恢复

### 根本原因
`clawdbot-local-memory` 插件未编译，导致 Gateway 配置验证失败：
```
plugin manifest not found: /home/neardws/clawd/plugins/clawdbot-local-memory/dist/openclaw.plugin.json
```

### Mac Mini 冗余备份失效原因
Mac Mini SSH 密钥未配置，无法远程连接 Ubuntu 执行修复：
```
Permission denied (publickey)
```

### 已修复
- ✅ 在 Mac Mini 生成了 SSH 密钥对
- ✅ 公钥已添加到 Ubuntu authorized_keys
- ✅ SSH 连接测试通过

---

## self-heal.py 脚本改进

### 新增功能
1. **插件编译检测** - 自动检查 `plugins.load.paths` 中的自定义插件
2. **自动编译修复** - 检测到未编译插件时自动 `npm install` + `npm run build`
3. **告警机制** - 连续3次修复失败发送通知
4. **失败计数** - 记录各组件失败次数
5. **`--check` 只检查不修复** - 避免意外重启 Gateway
6. **改进的 Gateway 健康检查** - 进程 + 端口 + HTTP 状态码三重验证（503 也算正常）

---

## Mac Mini 备用 Gateway 部署

### 配置
- OpenClaw CLI: v2026.2.2-3
- 备用端口: 19789
- 配置目录: `~/.openclaw-backup/`
- 故障切换脚本: `~/.clawdbot/gateway-failover.sh`
- Cron: 每 2 分钟检查

### 工作流程
1. Mac Mini 每 2 分钟检查 Ubuntu Gateway
2. 连续 3 次失败 → 启动备用 Gateway
3. Ubuntu 恢复后自动切回

---

## OpenClaw 更新

- 更新到最新 commit: `a749db982`
- 重要更新：
  - 🔒 安全修复: voice-call webhook 验证加固
  - 🔒 安全修复: channel metadata 与 system prompt 隔离
  - ✨ Discord: 新增 set-presence 功能
  - ✨ Telegram: 新增 model buttons 选择
  - ✨ Moonshot: 支持中国区 API

---

## 飞书（Feishu）插件配置

### 配置信息
- App ID: `cli_a9ff2a77faf8dbc4`
- Bot 名称: Axis AI
- 连接方式: WebSocket 长连接
- 群聊策略: open

### 已配对用户
- Feishu Open ID: `ou_d11a7d3f57f7cafa9ee70c566b9a1269`

### 注意事项
- 飞书妙记（Minutes）需要额外的 API 集成，当前插件不支持
- 需要 `aily:file:read` 权限才能读取妙记
